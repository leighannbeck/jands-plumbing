#name: Deploy via SSH
#'on':
#  - push
#  - repository_dispatch
#permissions:
#  actions: read
#  contents: read
#jobs:
#  deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#      - uses: actions/checkout@v4
#      # Make sure the @v0.9.0 matches the current version of the action
#      - uses: webfactory/ssh-agent@v0.9.0
#        with:
#          ssh-private-key: ${{ secrets.PRIVATE_KEY }}
#
#    - name: Checkout code
#       uses: actions/checkout@v4
#          - name: Setup Node.js
#            uses: actions/setup-node@v4
#            with:
#              node-version: '20'
#          - name: Install dependencies
#            run: npm install
#
#          - name: Build Vite site
#            run: npm run build
#
#          - name: Deploy to server
#            run: |
#              ssh-keyscan -H jandsplumbingheating.com >> ~/.ssh/known_hosts
#              scp -r ./dist/* lwalker32@jandsplumbingheating.com:/var/www/jandsplumbingheating.com/html

name: Deploy to Digital Ocean

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Deploy to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "dist/*"
          target: "/var/www/jandsplumbingheating.com/html"
          strip_components: 1